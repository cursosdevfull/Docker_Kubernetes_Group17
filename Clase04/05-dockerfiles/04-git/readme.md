# Git

```
docker run --rm -it -v $(pwd -W):/git alpine/git clone https://github.com/cursosdevfull/Docker_Kubernetes_Group17.git
```

- usen alpine/git
- clone un repositorio público
- crear un carpeta y vincularla como un volumen host y ahí deben quedar el repositorio clonado